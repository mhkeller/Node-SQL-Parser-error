Node SQL Parser error
===

## The error

When trying to parse this query, it should parse properly as a `SELECT` query. Instead it throws an error.

## Steps to reproduce

```sh
npm i
node index.js
```

The error will be in the console.
